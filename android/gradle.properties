# Flutter & Gradle settings
org.gradle.jvmargs=-Xmx4G -XX:+UseParallelGC -XX:MaxMetaspaceSize=1G

# 🔥 Activa soporte AndroidX
android.useAndroidX=true
android.enableJetifier=true

# Configuración de red y SSL
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=

# Desactivar verificación SSL (solo para desarrollo/debugging)
# ADVERTENCIA: No usar en producción
systemProp.javax.net.ssl.trustStoreType=jks

# Gradle daemon y cache
org.gradle.caching=true
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true
